import { BaseService, Sudo } from '../infrastructure';
export declare class Licence extends BaseService {
    all(options?: Sudo): Promise<import("../infrastructure").GetResponse>;
    create(options?: Sudo): Promise<object>;
}
